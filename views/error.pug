extends layout.pug
block content 
    nav.grid-container.portfolio-breadcrumb
        a(href='/') ← Back
    article.grid-container.portfolio-intro
        div 
        h1.text-light.font-righteous.wheat-text-shadow #{status} #{message}
        p.lead.text-light.wheat-text-shadow
        